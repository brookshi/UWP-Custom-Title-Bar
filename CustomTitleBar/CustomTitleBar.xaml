<UserControl
    x:Class="CustomTitleBar.CustomTitleBar"
    Name="CustomTitleBarControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CustomTitleBar"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="CustomTitleBarControl_Loaded">
    
    <Grid VerticalAlignment="Top" Height="{Binding TitleBarHeight, ElementName=CustomTitleBarControl, Mode=OneWay}" Canvas.ZIndex="10">
        <Rectangle DoubleTapped="TitleBarBackground_DoubleTapped" Name="TitleBarBackground" Fill="{Binding TitleBarBackgroundColor, ElementName=CustomTitleBarControl}"/>
        <Grid Padding="{Binding TitleBarPadding, ElementName=CustomTitleBarControl, Mode=OneWay}" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Margin="{Binding TitleMargin, ElementName=CustomTitleBarControl}" Text="{Binding Title, ElementName=CustomTitleBarControl}"/>
            <!--<ContentPresenter Grid.Column="1" Content="{Binding TitleBarControl, ElementName=CustomTitleBarControl}"/>-->
            <Button Grid.Column="1" Content="asdf"/>
        </Grid>
    </Grid>
</UserControl>
